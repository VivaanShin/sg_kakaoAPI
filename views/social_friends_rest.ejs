<!DOCTYPE html>
<html>

<head>
  <title>KakaoLink v2 Demo(Default / Feed) - Kakao JavaScript SDK</title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
  <script type="text/javascript" src="https://developers.kakao.com/sdk/js/kakao.min.js"></script>
  
    
</head>

<body>
        <div>카카오톡 친구 수 : <%= total_count %></div>
        <div>친구 리스트 중 즐겨찾기 친구 수 : <%= favorite_count %></div>

        <% if(after_url != null){ %>
        <div>다음 페이지 URL : <%= after_url %></div>
        <% } %>
    
        <% if(before_url != null){ %>
        <div>이전 페이지 URL : <%= before_url %></div>
        <% } %>
    
        <% for(var i=0; i<elements.length; i++){ %>
            <div>회원번호 : <%= elements[i].id %></div>
            <div>각 앱에서 친구마다 고유한 참고용 코드(메시지 전송 시 사용) : <%= elements[i].uuid %></div>
            <div>사용자 프로필 닉네임 : <%= elements[i].profile_nickname %></div>
            <div>사용자 프로필 썸네일 이미지 : <%= elements[i].profile_thumbnail_image %></div>
            <div>카카오톡 즐겨찾기 여부 : <%= elements[i].favorite %></div>
           
            <br>
        <% } %>
    
    
        
    
    
</body>

</html>
