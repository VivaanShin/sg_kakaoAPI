<!DOCTYPE html>
<html>
 <head>
  <meta charset="utf-8"/>
  <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
  <meta name="viewport" content="user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0, width=device-width"/>
  
  <script src="//developers.kakao.com/sdk/js/kakao.min.js"></script>
     <a id="kakao-login-btn"></a>
  
  <script type='text/javascript'>
    //<![CDATA[
   // 사용할 앱의 JavaScript 키를 설정해 주세요.
   Kakao.init('c242298ca638ef9255d40916e81a9b03');
     console.log("social_profile script 실행")
   
   // 카카오 로그인 버튼을 생성합니다.
   Kakao.Auth.createLoginButton({
    container: '#kakao-login-btn',
    success: function(authObj) {
     
     // 로그인 성공시, API를 호출합니다.
     Kakao.API.request({
      url: '/v1/api/talk/profile',
      success: function(res) {
       console.log(res);
       document.getElementById("nickName").innerHTML = res.nickName;
          document.getElementById("profileImageUrl").innerHTML = res.profileImageUrl;
          document.getElementById("thumbnailUrl").innerHTML = res.thumbnailUrl;
          document.getElementById("countryISO").innerHTML = res.countryISO;
       
      },
      fail: function(error) {
       alert(JSON.stringify(error));
      }
     });
    },
    fail: function(err) {
     alert(JSON.stringify(err));
    }
   });
    //]]>
  </script>
 
    
    </head>
 
 <body>
      <div>닉네임 : <span id="nickName"></span></div>
     <div>프로필이미지 : <span id="profileImageUrl"></span></div>
     <div>썸네일이미지 : <span id="thumbnailUrl"></span></div>
     <div>사용중인국가 : <span id="countryISO"></span></div>
     
     
</body>
</html>